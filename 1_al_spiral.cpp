/*

Problem Link:

*/

#include <algorithm>
#include <iostream>
#include <string>
#include <vector>

std::vector<int> spiralTraverse(std::vector<std::vector<int>> array) {
  auto rows = array.size();
  auto columns = array.at(0).size();

  return {};
}

int main() {
  std::cout << std::endl;
  return 0;
}